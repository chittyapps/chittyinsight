import { useState } from "react";
import { Bell, Crown, ChevronDown, Menu } from "lucide-react";
import { Button } from "@/components/ui/button";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";

export function NavigationHeader() {
  const [notificationsOpen, setNotificationsOpen] = useState(false);

  return (
    <header className="bg-chitty-surface border-b border-chitty-border sticky top-0 z-50 backdrop-blur-sm bg-opacity-95">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex justify-between items-center h-16">
          {/* Logo and Brand */}
          <div className="flex items-center space-x-4">
            <Button
              variant="ghost"
              size="sm"
              className="lg:hidden p-2"
              data-testid="button-mobile-menu"
            >
              <Menu className="h-5 w-5" />
            </Button>
            
            <div className="flex items-center space-x-2">
              <div className="w-8 h-8 bg-chitty-blue rounded-lg flex items-center justify-center">
                <i className="fas fa-brain text-white text-sm"></i>
              </div>
              <span className="text-xl font-bold">ChittyData</span>
            </div>
            
            <div className="hidden md:flex items-center space-x-1 text-sm text-gray-400">
              <span>AI Activity Intelligence</span>
              <div className="w-2 h-2 bg-chitty-success rounded-full animate-pulse-slow"></div>
              <span className="chitty-text-success text-xs">Live</span>
            </div>
          </div>

          {/* User Profile and Controls */}
          <div className="flex items-center space-x-4">
            {/* Notifications */}
            <Button
              variant="ghost"
              size="sm"
              className="relative p-2 rounded-lg hover:bg-chitty-border transition-colors"
              onClick={() => setNotificationsOpen(!notificationsOpen)}
              data-testid="button-notifications"
            >
              <Bell className="h-5 w-5 text-gray-400" />
              <span className="absolute -top-1 -right-1 w-3 h-3 bg-chitty-warning rounded-full text-xs"></span>
            </Button>
            
            {/* Role Indicator */}
            <div className="hidden sm:flex items-center space-x-2 px-3 py-1 bg-chitty-blue bg-opacity-20 rounded-full text-sm">
              <Crown className="h-3 w-3 text-chitty-blue" />
              <span className="text-chitty-blue" data-testid="text-user-role">Admin</span>
            </div>

            {/* User Menu */}
            <DropdownMenu>
              <DropdownMenuTrigger asChild>
                <Button 
                  variant="ghost" 
                  className="flex items-center space-x-2 p-2 rounded-lg hover:bg-chitty-border transition-colors"
                  data-testid="button-user-menu"
                >
                  <div className="w-8 h-8 bg-chitty-blue rounded-full flex items-center justify-center">
                    <span className="text-white text-sm font-medium" data-testid="text-user-initials">JD</span>
                  </div>
                  <ChevronDown className="h-3 w-3 text-gray-400" />
                </Button>
              </DropdownMenuTrigger>
              <DropdownMenuContent align="end" className="w-56 bg-chitty-surface border-chitty-border">
                <DropdownMenuItem className="hover:bg-chitty-border" data-testid="menu-item-profile">
                  <i className="fas fa-user mr-2 text-sm"></i>
                  Profile Settings
                </DropdownMenuItem>
                <DropdownMenuItem className="hover:bg-chitty-border" data-testid="menu-item-preferences">
                  <i className="fas fa-cog mr-2 text-sm"></i>
                  Preferences
                </DropdownMenuItem>
                <DropdownMenuSeparator className="bg-chitty-border" />
                <DropdownMenuItem className="hover:bg-chitty-border" data-testid="menu-item-help">
                  <i className="fas fa-question-circle mr-2 text-sm"></i>
                  Help & Support
                </DropdownMenuItem>
                <DropdownMenuSeparator className="bg-chitty-border" />
                <DropdownMenuItem className="hover:bg-chitty-border text-red-400" data-testid="menu-item-logout">
                  <i className="fas fa-sign-out-alt mr-2 text-sm"></i>
                  Log Out
                </DropdownMenuItem>
              </DropdownMenuContent>
            </DropdownMenu>
          </div>
        </div>
      </div>
    </header>
  );
}
